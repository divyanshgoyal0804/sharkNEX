# SMTP Configuration for Nodemailer (Zoho Mail)
# These are required for sending emails to sales@sharkspace.in

# Zoho SMTP Host (use smtp.zoho.in for India, smtp.zoho.com for US)
SMTP_HOST=smtp.zoho.in

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Set to 'true' for port 465 (SSL), 'false' for port 587 (TLS)
SMTP_SECURE=false

# Your Zoho email address (sender)
SMTP_USER=noreply@sharkspace.in

# Your Zoho password or App-Specific Password
# If 2FA enabled, generate App Password at: https://accounts.zoho.in/home#security/security_pwd
SMTP_PASSWORD=your-zoho-password
